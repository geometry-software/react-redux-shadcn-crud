export interface Post {
  id: number;
  title: string;
  body: string;
}

export interface Task {
  id: string;
  text: string;
  completed: boolean;
}

export interface Product {
  id: number;
  title: string;
  price: number;
}
